# Galactic Ledger — CI Pipeline
# This workflow will be configured once the tech stack is decided.
# Placeholder structure for common game dev CI tasks.

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # - name: Run linter
      #   run: echo "Configure linter for chosen tech stack"

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      # - name: Run tests
      #   run: echo "Configure test runner for chosen tech stack"

  # build:
  #   name: Build Game
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Build
  #       run: echo "Configure build step"
